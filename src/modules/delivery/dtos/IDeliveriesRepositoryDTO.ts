export default interface IDeliveriesRepositoryDTO {
  recipientId: string;
  postalCode: string;
  productId: string;
  productQuantity: number;
  address: string;
  neighborhood: string;
  city: string;
  state: string;
}
